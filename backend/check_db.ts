import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; import ws from 'ws'; dotenv.config(); const s = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY, { auth: { persistSession: false }, realtime: { transport: ws as any } }); Promise.all([s.from('employees').select('id, employment_status, position_id, ctc_annual'), s.from('positions').select('*')]).then(([e, p]) => { console.log('Emps:', e.data?.length, 'Positions:', p.data?.length); console.log('Active Emps:', e.data?.filter(x => x.employment_status === 'Active').length); console.log('Emps with no position_id:', e.data?.filter(x => !x.position_id).length); console.log('Employees:', e.data?.slice(0,2)); console.log('Positions:', p.data?.slice(0,2)); });
