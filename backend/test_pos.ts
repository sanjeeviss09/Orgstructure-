import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; import ws from 'ws'; dotenv.config(); const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY, { auth: { persistSession: false }, realtime: { transport: ws as any } }); supabase.from('positions').insert([{ id: 'P_TEST', title: 'Test', department: 'Test', business_unit: 'Test', status: 'A', budgeted_ctc: 1000 }]).then(res => console.log(res));
